import type { FC } from 'react'

type TProps = {}

const Authorization: FC<TProps> = () => {
  console.log(`Authorization component is working`)

  return (
    <div>
      <div />
    </div>
  )
}

export default Authorization
